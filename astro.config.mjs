import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // No specific Astro configurations needed for this simple static site yet.
  // If deploying to a subpath, you might need to set `site` and `base` options.
});
